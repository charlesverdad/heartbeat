# Older yt-dlp builds 403 on every Heartbeat stream (YouTube's SABR rollout).
# Keep this floor in sync with MIN_YTDLP_VERSION in video_downloader.py.
yt-dlp>=2026.08.17
webvtt-py>=0.4.6
openai-whisper
mlx-whisper; platform_machine == "arm64" and sys_platform == "darwin"
streamlit>=1.28.0
# Simplified -> Traditional for the zh-Hant subtitle track. The pure-Python
# reimplementation, not the C++ binding, so it needs no compiler in the nix shell.
opencc-python-reimplemented>=0.1.7
# Uploading caption tracks to YouTube (youtube/captions/yt_captions.py). The
# official clients rather than hand-rolled HTTP, because this is a write path to
# a public channel and token refresh is where hand-rolled code fails quietly.
google-api-python-client>=2.200.0
google-auth-oauthlib>=1.4.1
