apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis
    app.kubernetes.io/environment: dev
    component: cache
    environment: dev
  name: redis
  namespace: redis-dev
spec:
  ports:
  - name: redis
    port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: redis
    app.kubernetes.io/environment: dev
    environment: dev
  type: ClusterIP
