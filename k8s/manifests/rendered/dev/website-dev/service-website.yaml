apiVersion: v1
kind: Service
metadata:
  labels:
    app: website
    app.kubernetes.io/environment: dev
    environment: dev
  name: website
  namespace: website-dev
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 2368
  selector:
    app: website
    app.kubernetes.io/environment: dev
    environment: dev
  type: ClusterIP
