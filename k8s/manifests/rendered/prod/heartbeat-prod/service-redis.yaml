apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis
    app.kubernetes.io/environment: prod
    component: cache
    environment: prod
  name: redis
  namespace: heartbeat-prod
spec:
  ports:
  - name: redis
    port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: redis
    app.kubernetes.io/environment: prod
    environment: prod
  type: ClusterIP
